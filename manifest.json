{
  "manifest_version": 3,
  "name": "KeepTabs — never lose a tab",
  "version": "0.2.0",
  "description": "Save all your tabs in one click, with automatic versioned backups. Never lose them to an update, crash, or uninstall. Back up to a synced folder. No subscription, no account, local-first.",
  "permissions": ["tabs", "storage"],
  "background": { "service_worker": "background.js", "type": "module" },
  "action": { "default_title": "Save this window (KeepTabs)" }
}
